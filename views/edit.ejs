<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
</head>

<body class="bg-light">

    <div class="container py-5" style="max-width: 600px;">

        <div class="text-center mb-4">
            <h2 class="fw-bold text-primary d-flex align-items-center justify-content-center">
                <img src="https://cdn-icons-png.flaticon.com/512/992/992700.png" width="40" class="me-2">
                My Todo App
            </h2>
            <p class="text-muted mt-2">Stay organized and productive</p>
        </div>

        <div class="card shadow border-0">
            <div class="card-body p-4">

                <h3 class="text-center mb-4 text-primary">Edit Task</h3>

                <form action="/edit-task" method="POST">

                    <input type="hidden" name="id" value="<%= task.id %>">

                    <div class="mb-3">
                        <label class="form-label fw-semibold">Task Name</label>
                        <input type="text" class="form-control" name="taskName" value="<%= task.taskName %>" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-semibold">Priority</label>
                        <select name="taskPriority" class="form-select" required>
                            <option value="Low" <%=task.taskPriority==='Low' ? 'selected' : '' %>>Low</option>
                            <option value="Medium" <%=task.taskPriority==='Medium' ? 'selected' : '' %>>Medium</option>
                            <option value="High" <%=task.taskPriority==='High' ? 'selected' : '' %>>High</option>
                        </select>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="/" class="btn btn-outline-secondary">Cancel</a>
                        <button class="btn btn-primary">Save Changes</button>
                    </div>

                </form>

            </div>
        </div>
    </div>

</body>

</html>